mixin project-content-card(img, text, classModifier)
  div(class= classModifier.imgWrap ? 'project-card__img-wrap ' + classModifier.imgWrap : 'project-card__img-wrap ')
    img(src= img.src, alt= img.alt)
  .project-card__body
    h3.project-card__title #{text.title}
    .project-card__text-wrap
      each p in text.desc
        .text #{p}
    block

mixin card-content-with-list(img, text, classModifier)
  div(class= classModifier.imgWrap ? 'project-card__img-wrap ' + classModifier.imgWrap : 'project-card__img-wrap ')
    img(src= img.src, alt= img.alt)
  .project-card__body
    h3(class= classModifier.title ? 'project-card__title ' + classModifier.title : 'project-card__title ') #{text.title}
    ul.wrap-list-custom-marker
      each li in text.desc
        li(class='list-marker-color-custom')
          span.text #{li}
    block

mixin content-blog(date, title, text)
  .blog-card__img-wrap
    img(src="assets/img/index/news/news-card-picture-one.jpg", alt="пресс")
  .blog-card__description
    .text-small.blog-card__date #{date}
    h3.blog-card__title #{title}
    .blog-card__text-wrap
      .text #{text}
    .blog-card__interactions
      .blog-card__button-wrap
        a(href="#" class="cards-short-description__battom-link") Читать далее
      //- .blog-card__views
      //-   .blog-card__views-icon
      //-     include ../../../img/blog/eye.svg
      //-     span.text-small 17
      //- .blog-card__network
      //-   a(href="#" class="blog-card__network-link blog-card__network-link--facebook" data-title="Опубликовать статью в Facebook")
      //-   a(href="#" class="blog-card__network-link blog-card__network-link--vk" data-title="Опубликовать статью в ВКонтакте")
      //-   a(href="#" class="blog-card__network-link blog-card__network-link--instagram" data-title="Опубликовать статью в Instagram")
